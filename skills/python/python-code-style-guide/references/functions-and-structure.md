# 函数与结构规范

- 单函数职责单一，避免「既查数据又拼装响应又打日志」
- 参数数量建议不超过 5 个；超过时优先封装对象或拆分函数
- 函数须提供必要描述（如 docstring），至少说明用途、关键参数与返回值语义（复杂副作用或异常也应说明）；注释与 docstring 语言约定见 `readability.md`
- 尽早返回（guard clause）优于深层嵌套
- 异常处理要具体，避免裸 `except Exception` 吞错
